<Window x:Class="OnMuhasebeApp.Presentation.Views.CariAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yeni Cari Kartı" Height="450" Width="350" WindowStartupLocation="CenterOwner" Background="#F5F6F8">
    
    <StackPanel Margin="20">
        <TextBlock Text="Cari Ünvanı" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox Text="{Binding YeniCari.Unvan, UpdateSourceTrigger=PropertyChanged}" 
                 MaxLength="100" Height="30" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

        <TextBlock Text="Vergi Dairesi" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox Text="{Binding YeniCari.VergiDairesi, UpdateSourceTrigger=PropertyChanged}" 
                 PreviewTextInput="SadeceHarfEngeli"
                 MaxLength="50" Height="30" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

        <TextBlock Text="Vergi No / TCKN (10-11 Hane)" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox Text="{Binding YeniCari.VergiNo, UpdateSourceTrigger=PropertyChanged}" 
                 MaxLength="11" 
                 PreviewTextInput="SadeceRakamEngeli"
                 DataObject.Pasting="PastingHandler"
                 Height="30" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

        <TextBlock Text="Telefon (90...)" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox Text="{Binding YeniCari.Telefon, UpdateSourceTrigger=PropertyChanged}" 
                 MaxLength="12"
                 PreviewTextInput="SadeceRakamEngeli"
                 DataObject.Pasting="PastingHandler"
                 Height="30" Margin="0,0,0,20" VerticalContentAlignment="Center"/>

        <Button Content="Kaydet" Click="SaveButton_Click" Height="40" Background="#4CAF50" Foreground="White" FontWeight="Bold">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </StackPanel>
</Window>